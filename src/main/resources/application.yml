server:
  port: 8961

spring:
  application:
    name: CLOUD-GATEWAY
  profiles: cloud-gateway1
  cloud:
    config:
      # 对应分支
      label: master
      profile: prod
      discovery:
        service-id: CLOUD-CONFIG
        enabled: true
        
eureka:
  client:
    serviceUrl:
      defaultZone: http://172.16.246.128:8761/eureka,http://172.16.246.128:8762/eureka,http://172.16.246.128:8763/eureka