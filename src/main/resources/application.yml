server:
  port: 8961

spring:
  application:
    name: cloud-gateway
  profiles: gateway-server1
  cloud:
    config:
      fail-fast: true
      name: cloud-config
      label: master
      profile: prod
      discovery:
        service-id: cloud-config
        enabled: true